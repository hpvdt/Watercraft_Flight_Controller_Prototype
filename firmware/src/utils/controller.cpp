/*
pid controller class

constructor(initial setpoints (height, pitch, roll=0), gains, other params)
pid_correction(input measurements, output corrective control)
release_integral()
update_setpoints(new_height_setpoint, new_roll_setpoint)
*/